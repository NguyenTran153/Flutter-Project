name: Auto Push

on:
  push:
    branches:
      - 'milestone-4'
      - 'master'

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Push changes
        uses: github/action-push@master
        with:
          github_token: ${{ secrets.TOKEN }}
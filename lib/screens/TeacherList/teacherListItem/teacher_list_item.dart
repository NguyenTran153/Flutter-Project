import 'package:flutter/material.dart';

class TeacherListItem extends StatelessWidget {
  const TeacherListItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
